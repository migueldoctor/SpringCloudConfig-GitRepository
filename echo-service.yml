server:
  port: ${port:8091}
  ribbon:
    eureka:
      enabled: true
    ServerListRefreshInterval: 1500

eureka:
  instance:
    appname: echo-service
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  
